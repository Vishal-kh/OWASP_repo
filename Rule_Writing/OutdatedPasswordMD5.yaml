rules:
  - id: week-hashing-md5
    languages:
      - java
    severity: ERROR
    message: Weak password!the password is hashed using a weak or outdated algorithm
      like MD5.
    pattern-either:
      - pattern: "MessageDigest $MD=MessageDigest.getInstance(...);\r

          ...\r

          $MD.update(...);"
